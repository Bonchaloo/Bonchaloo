{
  "name": "watch",
  "version": "1.0.0",
  "private": true,
  "description": "Config-driven browser watcher: loads pages in Chromium, extracts values, commits the diff.",
  "type": "module",
  "scripts": {
    "watch": "node run.mjs",
    "selftest": "node selftest.mjs"
  },
  "dependencies": {
    "playwright": "^1.62.1"
  }
}
